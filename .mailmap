# SPDX-License-Identifier: AGPL-3.0-or-later
# Copyright © 2021 Pablo Hörtner <redtux@pm.me>
#
# Git mailmap
# See https://git-scm.com/docs/gitmailmap

# Prevent git from showing duplicate names with commands like "git shortlog"
# See "man git-shortlog" for details.
# The mapping syntax is:
# Name that should be used <email that should be used> Bad name <bad email>
#
# You can skip Bad name if it is the same as the one that should be used, and is unique.
#
# This file is up-to-date if the following command gives no duplicates.
# git log --format="%aN <%aE>" | sort -u

Pablo Hörtner <redtux@pm.me>          GitHub <noreply@github.com>
Pablo Hörtner <redtux@pm.me>          <redtux@users.noreply.github.com>
